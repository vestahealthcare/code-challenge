{
  "name": "code-challenge",
  "author": "rbaxter",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "prep": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll",
    "deploy": "gh-pages -d out -t true"
  },
  "dependencies": {
    "next": "latest",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "gh-pages": "^3.2.3"
  }
}
